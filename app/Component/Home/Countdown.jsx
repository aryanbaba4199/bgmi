import { View, Text } from 'react-native'
import React from 'react'

const Countdown = () => {
  return (
    <View>
      <Text>Countdown</Text>
    </View>
  )
}

export default React.memo(Countdown);